content/00-digital-hardware-foundations/10-microarchitectural-security/lessons/02-transient-execution-vulnerabilities/01-speculative-branch-attacks/06-branch-history-injection-mechanics.md
# Branch History Injection Mechanics and Global History Register Manipulation

To protect operating system kernels and secure hypervisors against speculative branch target hijacking attacks like Spectre Variant 2, microprocessor manufacturers introduced advanced hardware-enforced branch isolation mechanisms, most notably **Enhanced Indirect Branch Restricted Speculation (eIBRS)**. Under eIBRS, hardware architects modified the CPU's internal Branch Target Buffer (BTB)—the hardware cache responsible for predicting the jump targets of indirect branches—by tagging every BTB entry with its corresponding CPU privilege level (User Mode $PL=3$ versus Kernel Mode $PL=0$) and Process Address Space ID (PCID/ASID). When an unprivileged user process executes in user space, the hardware tags all predictions generated by the user process. When the user process subsequently executes a system call (`syscall`) and transitions into kernel space, the hardware eIBRS boundary checks the privilege tag of every BTB entry. If a BTB entry was created by user-mode code ($PL=3$), the kernel-mode fetch engine ($PL=0$) rejects the prediction, preventing user-space software from poisoning kernel indirect branch targets. Software security engineers believed that eIBRS had permanently solved speculative indirect branch hijacking. However, an un-isolated microarchitectural flaw remained hidden within the branch prediction pipeline: **The Global History Register (GHR)**. To predict conditional and indirect branch outcomes accurately, modern branch predictors do not rely solely on the branch instruction's virtual address; they XOR the branch address with a 32-bit to 194-bit hardware shift register—the Global History Register (also called the Branch History Buffer / BHB)—that records the precise sequence of Taken ($1$) and Not-Taken ($0$) outcomes of all recently executed branches. Crucially, while hardware architects tagged target entries in the BTB under eIBRS, **they did NOT isolate or clear the Global History Register across privilege transitions!** An unprivileged user-space process can execute a precise sequence of artificial conditional branches in user mode to craft an arbitrary binary pattern inside the shared GHR. When the process transitions into kernel mode, the GHR retains the attacker's manipulated history pattern. When the kernel executes an indirect branch, the manipulated GHR forces the predictor to calculate a prediction table index that collides with an existing, valid kernel-mode target entry pointing to an attacker-selected disclosure gadget. This vulnerability, known as **Branch History Injection (BHI)** or **Spectre-BHB**, proves that tagging BTB target entries is insufficient if the global branch history register used to index those entries remains un-isolated across security domains.

```text
BRANCH HISTORY INJECTION (BHI) OVERPASSING eIBRS

 User Space (PL = 3)                       Shared Hardware Branch Predictor
 ┌───────────────────────────┐             ┌───────────────────────────┐
 │ Executes 64 User Branches ├────────────►│ Global History Reg (GHR)  │
 │ (Crafts Artificial GHR)   │             │ Holds Attacker's Pattern! │
 └─────────────┬─────────────┘             └─────────────┬─────────────┘
               │                                         │
               ▼ Executes syscall (PL0 Transition)       │
 Kernel Space (PL = 0)                                   │
 ┌───────────────────────────┐                           │
 │ Kernel Indirect Branch    ├───────────────────────────┘
 │ (Address A_kernel)        │ Index = A_kernel XOR GHR_attacker
 └─────────────┬─────────────┘
               │ eIBRS Tag Check Passed (Target IS a Valid Kernel Entry!)
               ▼
 Predictor Selects Entry Pointing to Attacker's Kernel Gadget!
 CPU Speculatively Jumps to Gadget -> Exfiltrates Kernel Secrets!
```

---

## The Bank Teller's Combination Lock and the Shared Footstep Sensor

To build an intuitive, crystal-clear mental model of how Branch History Injection bypasses hardware BTB tagging by manipulating global history, let us consider an everyday analogy: a multi-tenant bank vault.

Imagine a secure bank vault (the Host CPU Core) protected by a smart combination lock (the Advanced Branch Predictor). The bank vault is shared between two different people who enter at different times:
1. **The Customer (The User-Space Attacker Process - $PL=3$)**: An unprivileged bank customer who deposits and withdraws personal items in the lobby.
2. **The Bank Manager (The Privileged Kernel Process - $PL=0$)**: A high-security executive who transports high-value treasury bonds inside the vault.

```text
THE BANK VAULT AND FOOTSTEP SENSOR METAPHOR

 Customer (User Attacker)                      Bank Manager (Kernel Victim)
 ┌───────────────────────────┐                 ┌───────────────────────────┐
 │ Unprivileged User         │                 │ High-Security Executive   │
 └─────────────┬─────────────┘                 └─────────────┬─────────────┘
               │                                             │
               └─── SHARED VAULT DOOR & LOBBY FLOOR SENSOR ──┘
```

To prevent customers from tampering with the vault door, the bank installs a security camera scanner (**eIBRS Hardware Tagging**). Every keycard used at the vault door is tagged with a color-coded security rank:
* Customer Keycards are colored **RED** (User Mode $PL=3$).
* Manager Keycards are colored **GOLD** (Kernel Mode $PL=0$).

When a keycard is inserted into the lock, the lock checks the color tag. If a customer's RED keycard attempts to open a GOLD vault setting, the lock blocks the access instantly (**eIBRS Tag Rejection**). The bank manager believes the vault is $100\%$ immune to customer tampering!

However, the smart combination lock has a hidden secondary input sensor: an acoustic floor sensor embedded in the lobby carpet that records the **rhythm of footsteps taken in the lobby over the past 5 minutes** (the **Global History Register / GHR**).

To determine which vault room to open, the smart lock combines two inputs:
$$\text{Lock Combination Index} = \text{Keycard Number} \quad \mathbf{\text{XOR}} \quad \text{Lobby Footstep Rhythm}$$

```text
THE HIDDEN COMBINATION FORMULA

 Keycard Number (Color-Tagged by eIBRS)
                │
                ├──► [ XOR Combination Gate ] ──► Target Vault Room
                │
 Lobby Footstep Sensor (Un-isolated GHR!)
```

Now, trace how the customer executes a **Branch History Injection Attack**:

### Phase 1: Manipulating the Footstep Sensor (GHR Manipulation)
The customer cannot forge a GOLD manager keycard. But the customer **CAN STAMP THEIR FEET IN THE LOBBY**!

Before the manager arrives, the customer stands in the lobby and executes a precise, artificial footstep rhythm: *Tap-Tap-Pause-Tap-Tap-Pause* (**Executing a sequence of conditional branches in User Space**). 

The acoustic floor sensor records this artificial footstep rhythm into its 5-minute memory bank (**Writing an artificial bit pattern into the GHR**).

### Phase 2: The Manager Arrives and the Hijack Occurs
At 5:00 PM, the customer leaves the lobby, and the bank manager arrives (**System Call Execution**).

The manager approaches the vault door to open a routine storage room (**Executing an Indirect Branch at Address $A_{\text{kernel}}$**). The manager inserts their valid **GOLD Manager Keycard**.

The smart lock reads the manager's keycard:
1. **eIBRS Tag Check**: The lock sees a valid GOLD keycard ($PL=0$). Security check passed!
2. **Combination Index Calculation**: The lock takes the manager's keycard number and **XORs it with the customer's artificial footstep rhythm** still stored in the floor sensor!

$$\text{Calculated Index} = \text{Manager\_Keycard} \ \oplus \ \text{Customer\_Footstep\_Rhythm}$$

3. **The Speculative Hijack**: The calculated index points to an old, forgotten side vault containing confidential treasury records (**An Attacker-Selected Kernel Gadget**)!
4. The vault door speculatively swings open to the side vault! The manager speculatively steps inside, where an accomplice waiting in the shadows snaps a photo of the confidential records and leaves a Chocolate Bar on the lobby counter (**L1 Cache Fill**).

```text
SPECULATIVE VAULT HIJACK

 Manager Inserts Valid GOLD Keycard ──► Lock XORs with Customer's Footstep Rhythm!
                                      ──► Calculated Index Points to Side Vault!
                                      │
                                      ▼
 Vault Door Opens to Side Vault! ────► Manager steps in speculatively!
 Accomplice snaps photo of records ──► Leaves Chocolate Bar on Lobby Counter!
```

### Phase 3: The Reset and Exfiltration
10 seconds later, the manager checks their paper notebook, realizes the door opened to the wrong room, steps out, and resets the lock (**ROB Pipeline Flush / Architectural Rollback**).

To an official auditor, the manager never entered the side vault. No official files were moved.

**The Leak**: But the accomplice ALREADY has the photo, and the Chocolate Bar is STILL sitting on the lobby counter! The customer returns, checks the counter, sees the Chocolate Bar, and reads the secret treasury data!

Look at what occurred in this bank:
* The camera system (eIBRS) worked perfectly and verified the manager's GOLD keycard.
* The customer never forged a GOLD keycard.
* Yet, the customer **manipulated the shared floor sensor (the GHR)**, forcing the lock to combine the manager's valid keycard with the customer's artificial rhythm, speculatively opening the wrong vault room and leaking the secret!

This bank vault scenario is the exact physical analogue of **Branch History Injection (BHI)**:
* The bank vault is the **Physical CPU Core**.
* The customer is the **Unprivileged User Process ($PL=3$)**.
* The bank manager is the **Privileged Kernel Process ($PL=0$)**.
* The security camera is **eIBRS Hardware Tagging**.
* The acoustic floor sensor is the **Global History Register (GHR)**.
* Stamping feet in the lobby is **Executing a Branch Sequence in User Space**.
* The manager's GOLD keycard is the **Kernel Indirect Branch Address ($A_{\text{kernel}}$)**.
* The calculated combination index is the **Prediction Table Lookup Index ($A_{\text{kernel}} \oplus \text{GHR}$)**.
* Opening the side vault is **Speculatively Executing a Kernel Disclosure Gadget**.
* The Chocolate Bar left on the counter is the **Persistent Speculative L1 Cache Footprint**.

---

## Predictor Architecture: TAGE, Gshare, and the Global History Register

To understand why the Global History Register is shared and how it interacts with branch prediction lookup tables, we must inspect the internal microarchitecture of modern CPU branch predictors.

### Why Branch Predictors Use Global History

In early processors, branch predictors evaluated each branch instruction independently based solely on that branch's own past outcomes (Bimodal Prediction).

However, in complex software algorithms, the direction of a branch is frequently **correlated with the outcomes of completely different branches** executed earlier in the program flow.

Consider this simple software example:

```c
// Correlated Branch Execution Example
if (x == 0) {       // Branch 1
    do_something();
}
if (y == 0) {       // Branch 2
    do_other();
}
if (x + y == 0) {   // Branch 3 (Correlated with Branch 1 AND Branch 2!)
    // Branch 3 is TAKEN IF AND ONLY IF Branch 1 was TAKEN AND Branch 2 was TAKEN!
}
```

Notice Branch 3:
If the CPU knows that Branch 1 was **TAKEN** ($x = 0$) and Branch 2 was **TAKEN** ($y = 0$), the CPU can predict Branch 3 with $100\%$ accuracy!

To capture these inter-branch correlations, hardware architects introduced the **Global History Register (GHR)**.

---

### Anatomy of the Global History Register (GHR / BHB)

The **Global History Register (GHR)** (also called the Branch History Buffer / BHB) is a hardware shift register embedded within the CPU's Instruction Fetch unit.

```text
GLOBAL HISTORY REGISTER (GHR) SHIFT REGISTER ARCHITECTURE

 Bit 63 (Oldest Branch)                                     Bit 0 (Most Recent)
 ┌───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┬───┐
 │ 1 │ 0 │ 1 │ 1 │ 0 │ 0 │ 1 │ 0 │ 1 │ 1 │ 1 │ 0 │ 1 │ 0 │ 0 │ 1 │
 └───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┴───┘
   ▲                                                       ▲
   │ Shift Left (Oldest Bit Lost)                          │ New Outcome Shifted In!
   └───────────────────────────────────────────────────────┴── (1 = Taken, 0 = Not-Taken)
```

The GHR width varies across processor microarchitectures:
* **Intel Skylake / Kaby Lake**: $64\text{-bit}$ GHR.
* **Intel Ice Lake / Alder Lake**: $194\text{-bit}$ GHR.
* **AMD Zen 3 / Zen 4**: $64\text{-bit}$ to $128\text{-bit}$ GHR.
* **ARM Cortex-A78 / Neoverse N2**: $64\text{-bit}$ GHR.

Every time *any* conditional branch instruction completes execution in the CPU pipeline:
1. If the branch was **TAKEN**, the GHR shifts left by 1 bit and inserts a **$1$** at Bit 0:
   $$\text{GHR}_{\text{next}} = (\text{GHR}_{\text{current}} \ll 1) \ \mid \ 1$$
2. If the branch was **NOT TAKEN**, the GHR shifts left by 1 bit and inserts a **$0$** at Bit 0:
   $$\text{GHR}_{\text{next}} = (\text{GHR}_{\text{current}} \ll 1) \ \mid \ 0$$

---

### Gshare and TAGE Prediction Table Indexing

In advanced branch predictors (such as **Gshare** or **TAGE - TAgged GEometric History Length** predictors), the CPU predicts the target of an indirect branch by combining the branch's virtual instruction address ($A_{\text{branch}}$) with the Global History Register ($\text{GHR}$).

The lookup index ($I_{\text{table}}$) into the Branch Target Buffer or Pattern History Table is calculated using bitwise XOR hashing:

$$\mathbf{I_{\text{table}} = (A_{\text{branch}} \gg \text{Shift}) \ \mathbf{\oplus} \ \text{GHR} \ \ \mathbf{\&} \ \ (N_{\text{entries}} - 1)}$$

Where:
* $I_{\text{table}}$ is the integer index ($0 \dots N_{\text{entries}}-1$) selecting a specific prediction slot.
* $A_{\text{branch}}$ is the 64-bit virtual instruction address of the indirect branch.
* $\text{GHR}$ is the current 64-bit binary vector stored in the Global History Register.
* $\oplus$ is the bitwise XOR operator.
* $N_{\text{entries}}$ is the total number of slots in the prediction table (e.g., $N_{\text{entries}} = 4,096 = 2^{12}$).

```text
GSHARE / TAGE HARDWARE INDEXING DATAPATH

 Virtual Address of Branch (A_branch)
 ┌─────────────────────────────────────────────────────────────┐
 │ Extract Lower Bits [15:4]                                   │
 └─────────────┬───────────────────────────────────────────────┘
               │
               ├───────────────────► [ Bitwise XOR Gate ]
               │                            ▲
 Global History Register (GHR) ─────────────┘
 (Contains Attacker's Manipulated Pattern!) │
                                            ▼
                                  Table Lookup Index I_table
                                            │
                                            ▼
                       Selects Prediction Entry in Hardware Table!
```

Look at the indexing equation:
The lookup index $I_{\text{table}}$ depends on **BOTH $A_{\text{branch}}$ AND $\text{GHR}$**!

Even if $A_{\text{branch}}$ is fixed and protected by eIBRS, **changing the bits in $\text{GHR}$ alters $I_{\text{table}}$ completely**!

---

## How eIBRS Works and Why It Fails Against BHI

To understand Branch History Injection, we must first analyze how **Enhanced IBRS (eIBRS)** was intended to defend processors against Spectre Variant 2.

### The Mechanics of eIBRS Hardware BTB Tagging

Under eIBRS, hardware architects updated the Branch Target Buffer (BTB) cache array by adding two new fields to every BTB entry:
1. **Privilege Mode Tag ($\text{PL}$)**: Stores the CPU privilege level ($PL=3$ for User Mode, $PL=0$ for Kernel Mode).
2. **Address Space ID Tag ($\text{PCID / ASID}$)**: Stores the process address space identifier.

```text
EIBRS-ENHANCED BTB CACHE ENTRY STRUCTURE

 ┌──────────┬───────────┬──────────────┬───────────────────────────────┐
 │ Valid (V)│ PL Tag    │ PCID Tag     │ Target Address                │
 ├──────────┼───────────┼──────────────┼───────────────────────────────┤
 │    1     │ PL=0 (K)  │ PCID = 0x01  │ 0xFFFFFFFF_8100_9000 (Gadget) │
 └──────────┴───────────┴──────────────┴───────────────────────────────┘
  ▲          ▲           ▲
  └──────────┴───────────┴── Hardware checks these tags on EVERY branch!
```

When an indirect branch instruction executes at kernel address $A_{\text{kernel}}$ in Kernel Mode ($PL=0$):
1. The CPU queries the BTB.
2. The BTB checks the entry's privilege tag:
   $$\text{Hit Condition} \iff (\text{Tag}_{\text{addr}} == A_{\text{kernel}}) \ \mathbf{\&} \ (\text{Tag}_{\text{PL}} == 0) \ \mathbf{\&} \ (\text{Tag}_{\text{PCID}} == \text{Current\_PCID})$$
3. If an unprivileged user process ($PL=3$) attempted to poison the BTB entry, the entry's tag reads $\text{Tag}_{\text{PL}} = 3$.
4. The kernel-mode query sees $\text{Tag}_{\text{PL}} (3) \neq \text{Current\_PL} (0)$, **rejects the prediction**, and prevents the speculative jump!

---

### The Fundamental Flaw: The Un-Isolated GHR

Why does eIBRS fail to prevent Branch History Injection?

Because eIBRS tags **only the BTB entries**! eIBRS does **NOT** tag or isolate the Global History Register ($\text{GHR}$)!

```text
THE EIBRS SECURITY GAP

 ┌─────────────────────────────────────────────────────────────┐
 │ EIBRS HARDWARE PROTECTION (BTB Entries)                     │
 │  * BTB Target Entries ARE TAGGED with PL=0 vs PL=3.         │
 │  * User Mode PL=3 CANNOT create a valid PL=0 BTB entry!     │
 ├─────────────────────────────────────────────────────────────┤
 │ UN-PROTECTED GHR SHIFT REGISTER                             │
 │  * Global History Register IS NOT TAGGED!                   │
 │  * User Mode PL=3 CAN MANIPULATE the shared GHR bits!       │
 └─────────────────────────────────────────────────────────────┘
  (Attacker uses GHR manipulation to collide with EXISTING kernel BTB entries!)
```

Trace the flaw in eIBRS:
1. An attacker running in User Mode ($PL=3$) cannot create a new BTB entry marked $PL=0$.
2. **BUT THE KERNEL ALREADY CONTAINS VALID $PL=0$ BTB ENTRIES!** The kernel binary contains thousands of legitimate $PL=0$ BTB entries created during prior kernel execution!
3. Among these thousands of legitimate kernel BTB entries sits a valid $PL=0$ entry pointing to a **Kernel Disclosure Gadget ($A_{\text{gadget}}$)**.
4. By executing a calculated sequence of branches in User Mode ($PL=3$), the attacker manipulates the shared $\text{GHR}$ so that when the kernel executes an indirect branch at $A_{\text{kernel}}$, the XOR sum $A_{\text{kernel}} \oplus \text{GHR}_{\text{manipulated}}$ **collides with the index of the existing kernel gadget entry!**
5. The BTB reads the entry at that collided index. The entry's tag matches $\text{PL}=0$ (because it was created by the kernel earlier!).
6. **eIBRS Tag Check PASSES!** The CPU speculatively jumps to the kernel gadget!

The attacker bypassed eIBRS without ever creating a fake $PL=0$ entry, purely by **steering the kernel's index lookup to an existing kernel entry via GHR manipulation!**

---

## Step-by-Step BHI (Spectre-BHB) Attack Execution Protocol

Let us trace the complete step-by-step execution protocol of a Branch History Injection (BHI) attack:

```text
BRANCH HISTORY INJECTION (BHI) 5-PHASE ATTACK PROTOCOL

 Phase 1: Target Index Engineering ──► Find GHR_pattern such that:
                                       Index = A_kernel XOR GHR_pattern == Index(A_gadget)
                                       │
                                       ▼
 Phase 2: User GHR Manipulation    ──► Execute H conditional branches in User Space
                                       to shift GHR_pattern into the hardware GHR!
                                       │
                                       ▼
 Phase 3: Trigger System Call      ──► Execute 'syscall' transition to Kernel Mode (PL=0).
                                       (GHR retains GHR_pattern across transition!)
                                       │
                                       ▼
 Phase 4: Kernel Speculative Jump  ──► Kernel executes indirect branch at A_kernel.
                                       Index calculation selects BTB Entry for A_gadget!
                                       CPU speculatively executes Gadget in Kernel Mode!
                                       │
                                       ▼
 Phase 5: Side-Channel Exfiltration ──► Reload probe array in user space via Flush+Reload.
                                       (Exfiltrates kernel secret byte S!)
```

---

### Step-by-Step Hardware Datapath Walkthrough:

#### Step 1: Target Index Engineering
The attacker selects a target kernel indirect branch $A_{\text{kernel}}$ and identifies an existing kernel BTB entry pointing to a disclosure gadget $A_{\text{gadget}}$ located at table index $I_{\text{target}}$.

The attacker calculates the required $64\text{-bit}$ history pattern ($\text{GHR}_{\text{target}}$) that satisfies:

$$\mathbf{(A_{\text{kernel}} \gg \text{Shift}) \ \mathbf{\oplus} \ \text{GHR}_{\text{target}} \ \ \mathbf{\&} \ \ (N_{\text{entries}} - 1) == I_{\text{target}}}$$

Solving for $\text{GHR}_{\text{target}}$:

$$\mathbf{\text{GHR}_{\text{target}} = (A_{\text{kernel}} \gg \text{Shift}) \ \mathbf{\oplus} \ I_{\text{target}}}$$

---

#### Step 2: User-Space GHR Manipulation Loop
The attacker executes a precise sequence of $H$ conditional branch instructions in user space ($PL=3$) whose outcomes ($1$ for Taken, $0$ for Not-Taken) construct the exact binary bitstream of $\text{GHR}_{\text{target}}$:

```c
// User-space GHR Manipulation Function (Crafting GHR_target)
void inject_global_branch_history(uint64_t ghr_target, int history_length) {
    for (int i = history_length - 1; i >= 0; i--) {
        // Extract bit i of ghr_target
        int bit = (ghr_target >> i) & 1;
        
        // Execute conditional branch that evaluates to 'bit'
        if (bit) {
            // Branch TAKEN -> Shifts '1' into hardware GHR
            asm volatile ("nop");
        } else {
            // Branch NOT TAKEN -> Shifts '0' into hardware GHR
            asm volatile ("nop");
        }
    }
}
```

After this loop executes, the CPU's physical GHR contains **$\text{GHR}_{\text{target}}$**!

---

#### Step 3: Triggering the Kernel System Call
Immediately after completing the GHR manipulation loop, the attacker executes a system call (`syscall` or `int 0x80`):
1. The CPU context-switches from User Mode ($PL=3$) to Kernel Mode ($PL=0$).
2. **The Un-Isolated GHR Persistence**: The physical GHR shift register **retains $\text{GHR}_{\text{target}}$** across the privilege boundary!

---

#### Step 4: Kernel Speculative Execution Hijacking
1. The kernel handles the system call and reaches the target indirect branch $A_{\text{kernel}}$.
2. The function pointer misses in cache $\implies$ Branch evaluation stalls waiting for DRAM ($160\text{ cycles}$).
3. The CPU fetch unit queries the branch predictor, calculating the lookup index:
   $$I_{\text{lookup}} = (A_{\text{kernel}} \gg \text{Shift}) \ \mathbf{\oplus} \ \text{GHR}_{\text{target}} == I_{\text{target}}$$
4. The predictor queries table index $I_{\text{target}}$, finds the existing kernel entry pointing to $A_{\text{gadget}}$, and checks eIBRS tags:
   $$\text{Tag}_{\text{PL}} == 0 \quad (\mathbf{\text{eIBRS Check PASSES because entry WAS created by kernel!}})$$
5. The CPU **speculatively jumps to $A_{\text{gadget}}$ in Kernel Mode ($PL=0$)**!

```text
BHI SPECULATIVE EXECUTION TIMELINE

 Cycle 0   : Kernel Indirect Branch at A_kernel (DRAM Miss -> 160 Cycle Delay)
 Cycle 1   : Predictor XORs A_kernel with GHR_target -> Calculates Index I_target!
 Cycle 2   : Predictor fetches Entry I_target -> Target = A_gadget (eIBRS Tag = PL0 PASSES!)
 Cycle 3   : CPU Speculatively Jumps to A_gadget in Kernel Mode!
 Cycle 6   : Gadget reads Kernel Secret Byte S = 88 ('X') from RDI
 Cycle 9   : Gadget fetches Line 88 of Probe Array into L1 Data Cache!
 Cycle 160 : Target pointer arrives from DRAM -> Real Target = 0xFFFFFFFF_8105_2000!
 Cycle 161 : ROB FLUSH FIRED! Registers reset! BUT Line 88 STAYS IN L1 DATA CACHE!
```

6. The gadget speculatively reads kernel secret byte $S = 88_{10} = \text{'X'}$ and fetches line 88 of the probe array into L1 Data Cache.
7. At Cycle 160, the real target pointer arrives from DRAM. The CPU detects the misprediction, flushes the ROB, and resets registers.
8. Control returns to user space. The attacker reloads the probe array using Flush+Reload, measuring an L1 Cache Hit on line 88 and exfiltrating the kernel secret byte: **$S = 88 = \text{'X'}$**!

---

## Hardware and Software Mitigations

Because Branch History Injection bypasses early eIBRS hardware isolation, CPU foundries and operating system developers implemented two major mitigation layers: **Software BHB Clearing** and **Hardware History Isolation (ARM CSV2.2 / Intel eIBRS Extensions)**.

```text
BHI MITIGATION TAXONOMY

                          BHI MITIGATION STRATEGIES
                                      │
         ┌────────────────────────────┴────────────────────────────┐
         ▼                                                         ▼
 SOFTWARE BHB CLEARING LOOP (Software)             HARDWARE HISTORY ISOLATION (Silicon)
 * Overwrites GHR with 32/64 dummy branches        * Hardware automatically clears or tags GHR
   on every kernel entry (syscall/interrupt).        on privilege transitions (PL3 -> PL0).
```

---

### Mitigation 1: Software BHB Clearing Sequence (History Stuffing)

To defend existing processors (Intel Skylake through Alder Lake, ARM Cortex-A72 through A78) against BHI without replacing silicon, operating system kernels (Linux kernel `SPECTRE_BHI`) execute an **explicit BHB Clearing Loop** on every user-to-kernel transition (`syscall` entry, interrupt handler, hardware exception):

```assembly
; Linux Kernel BHB Clearing Loop (Executed on Syscall Entry)
; Overwrites the 64-bit GHR with a fixed dummy pattern in software!

.align 16
bhb_clear_loop:
    mov ecx, 32                 ; Execute 32 dummy branches
1:  b 2f                        ; Unconditional branch 1
2:  b 3f                        ; Unconditional branch 2
3:  sub ecx, 1
    jnz 1b                      ; Loop until GHR is completely overwritten!
    isb                         ; Instruction Synchronization Barrier
```

```text
SOFTWARE BHB CLEARING LOOP MECHANICS

 User-to-Kernel Syscall Transition
               │
               ▼
 Kernel executes 32 dummy branches in a loop
 ┌─────────────────────────────────────────────────────────────┐
 │ GLOBAL HISTORY REGISTER (GHR SHIFT REGISTER)                │
 │ Attacker's GHR_pattern is SHIFTED OUT AND OVERWRITTEN!      │
 │ GHR now holds 100% Cleared Dummy Pattern!                   │
 └─────────────────────────────────────────────────────────────┘
  (Attacker's footstep pattern erased before any kernel indirect branch runs!)
```

#### How BHB Clearing Works:
1. On every `syscall` or interrupt entry, before executing any kernel indirect branches, the kernel executes 32 or 64 dummy conditional branches.
2. The dummy branch outcomes shift into the physical GHR, **shifting out and erasing the attacker's $\text{GHR}_{\text{target}}$ pattern**.
3. When the kernel later executes its indirect branch $A_{\text{kernel}}$, the GHR contains safe, uniform dummy bits, preventing the index collision!

#### Performance Cost:
Executing 32 to 64 dummy branches on every system call adds **$40 \text{ to } 100\text{ CPU clock cycles}$** to every kernel transition, reducing system call throughput by **$8\%\text{ to } 20\%$**.

---

### Mitigation 2: Hardware History Isolation (ARM CSV2.2 / Intel eIBRS Extensions)

In newer CPU microarchitectures (ARMv8.5-A+ with CSV2.2/CSV2.3 and modern Intel/AMD processors):
* **Hardware GHR Reset on Privilege Transition**: The CPU hardware automatically **clears or resets the physical GHR shift register to zero** whenever the execution privilege level transitions from $PL=3$ (User) to $PL=0$ (Kernel).
* **Zero Software Overhead**: Hardware resets the GHR in **$1\text{ clock cycle}$** during the `syscall` context switch, eliminating the software BHB clearing loop and restoring $100\%$ bare-metal system call performance!

---

## Solved Industrial Engineering Exercise: Quantitative GHR Hashing Analysis, BHI Index Collision, and BHB Clearing Mitigation Verification

To consolidate your complete mastery of Branch History Injection (BHI), Gshare/TAGE GHR XOR index hashing, cross-domain aliasing, and BHB clearing loop mitigations, we will now walk through a complete, step-by-step industrial hardware engineering problem.

---

### Scenario and Parameters

You are a principal microarchitectural security engineer auditing a $3.2\text{ GHz}$ 64-bit server processor core ($T_{\text{clk}} = 0.3125\text{ ns} = 312.5\text{ ps}$).

The processor operates an Advanced Gshare Branch Predictor with the following parameters:
* **Prediction Table Capacity**: $N_{\text{entries}} = 4,096\text{ slots}$ ($2^{12} = 4,096 \implies 12\text{-bit Index}$).
* **Global History Register (GHR) Width**: $32\text{ Bits}$.
* **Index Hashing Formula**:
  $$I_{\text{table}} = (A_{\text{branch}}[15:4]) \ \mathbf{\oplus} \ \text{GHR}[11:0]$$
* **Hardware eIBRS**: Enabled (Tags BTB entries with $PL=0$ vs $PL=3$).

```text
3.2 GHz SERVER PROCESSOR WITH GSHARE PREDICTOR & eIBRS

 User Attacker Process (PL = 3) ──► [ Gshare Predictor (12-bit Index) ] ──► Kernel Syscall (PL = 0)
 Clock T = 312.5 ps                  Index = A_branch[15:4] XOR GHR[11:0]   Kernel Branch @ 0x8140_1040
                                     Target Gadget @ 0x8100_9000 (Index 0x123)
```

#### Memory Addresses:
* Kernel Indirect Branch Address: $A_{\text{kernel}} = \mathbf{\text{0xFFFFFFFF\_8140\_1040}}$.
* Target Kernel Disclosure Gadget: $A_{\text{gadget}} = \mathbf{\text{0xFFFFFFFF\_8100\_9000}}$.
* Target Index for $A_{\text{gadget}}$ in Prediction Table: $I_{\text{target}} = \mathbf{\text{0x123}} = 0001\_0010\_0011_2$.
* Target Kernel Secret Byte: Stored at address `0xFFFFFFFF_8180_5000` (Secret byte $S = 77_{10} = \text{0x4D} = \text{'M'}$).

#### Your Objective

1. Calculate the 12-bit virtual address bitfield $A_{\text{kernel}}[15:4]$ for $A_{\text{kernel}} = \text{0xFFFFFFFF\_8140\_1040}$.
2. Calculate the exact 12-bit history pattern ($\text{GHR}_{\text{pattern}}$) that an attacker in user space must inject into the GHR so that $A_{\text{kernel}}$ collides with $I_{\text{target}} = \text{0x123}$ in the prediction table.
3. Trace the clock cycle execution timeline ($t_0 \dots t_4$) when the kernel executes $A_{\text{kernel}}$ with $\text{GHR}_{\text{pattern}}$ active:
   * Show that eIBRS privilege checks pass because $A_{\text{gadget}}$ is a valid $PL=0$ kernel entry.
   * Show the CPU speculatively jumping to $A_{\text{gadget}}$ and loading probe line `T[77]` into L1 Data Cache **before the ROB flush fires at cycle 160**.
4. Calculate the reload timing delta measured by the attacker reloading `T[77]` vs un-accessed probe lines `T[k]`.
5. Verify the security impact when the Linux kernel executes a **BHB Clearing Loop** (32 branches) on `syscall` entry:
   * Show that BHB clearing forces $\text{GHR} \Leftarrow \text{0x000}$, calculating the safe non-colliding lookup index $I_{\text{safe}}$.
   * Calculate the execution time penalty (in nanoseconds) added to every system call by the BHB clearing loop.
6. Verify mathematical, structural, and timing correctness.

---

### Step-by-Step Derivation

#### Step 1: Calculate $A_{\text{kernel}}[15:4]$ and Required $\text{GHR}_{\text{pattern}}$

##### 1. Extract $A_{\text{kernel}}[15:4]$ from $A_{\text{kernel}} = \text{0xFFFFFFFF\_8140\_1040}$:
* Lowest 16 bits of $A_{\text{kernel}} = \text{0x1040} = 0001\_0000\_0100\_0000_2$.
* Shift right by 4 bits ($A_{\text{kernel}} \gg 4$):
  $$\text{0x8140\_1040} \gg 4 = \text{0x8140104}$$
* Extract lowest 12 bits ($[11:0]$):
  $$A_{\text{kernel}}[15:4] = \text{0x104} \ \& \ \text{0xFFF} = \text{0x104} = \mathbf{0001\_0000\_0100_2}$$

##### 2. Solve for $\text{GHR}_{\text{pattern}}$:
We apply the Gshare indexing equation:

$$I_{\text{table}} = A_{\text{kernel}}[15:4] \ \mathbf{\oplus} \ \text{GHR}_{\text{pattern}} == I_{\text{target}}$$

$$\text{0x104} \ \mathbf{\oplus} \ \text{GHR}_{\text{pattern}} == \text{0x123}$$

XORing both sides with `0x104`:

$$\text{GHR}_{\text{pattern}} = \text{0x104} \ \mathbf{\oplus} \ \text{0x123}$$

Convert to 12-bit binary:
* $\text{0x104} = 0001\_0000\_0100_2$
* $\text{0x123} = 0001\_0010\_0011_2$

$$\text{GHR}_{\text{pattern}} = (0001\_0000\_0100_2) \ \mathbf{\oplus} \ (0001\_0010\_0011_2) = 0000\_0010\_0111_2 = \mathbf{\text{0x027}}$$

$$\mathbf{\text{GHR}_{\text{pattern}}[11:0] = \text{0x027} = \text{12'b0000\_0010\_0111}_2}$$

##### Verification:
$$\text{0x104} \ \mathbf{\oplus} \ \text{0x027} = (0001\_0000\_0100_2) \ \mathbf{\oplus} \ (0000\_0010\_0111_2) = 0001\_0010\_0011_2 = \mathbf{\text{0x123} \quad (I_{\text{target}}!)}$$

When the attacker injects history pattern **`0x027`** into the GHR in user space, the kernel branch at `0x8140_1040` calculates index **`0x123`**!

---

#### Step 2: Trace Speculative Execution Timeline

1. **Cycle 0 ($t = 0.0\text{ ns}$)**: Kernel executes indirect branch at $A_{\text{kernel}} = \text{0xFFFFFFFF\_8140\_1040}$. Function pointer misses in cache $\implies$ Memory read issued to DRAM ($T_{\text{DRAM\_miss}} = 160\text{ cycles}$).
2. **Cycle 1 ($t = 0.3125\text{ ns}$)**: Predictor XORs address `0x104` with $\text{GHR} = \text{0x027}$, calculating $I_{\text{lookup}} = \text{0x123}$.
3. **Cycle 2 ($t = 0.6250\text{ ns}$)**: Predictor fetches Entry `0x123` from prediction table $\implies$ Target $= \mathbf{\text{0xFFFFFFFF\_8100\_9000} \quad (A_{\text{gadget}})}$.
   * **eIBRS Privilege Check**: Entry `0x123` has $\text{Tag}_{\text{PL}} = 0$ (created by kernel previously!) $\implies$ **eIBRS CHECK PASSES!**
4. **Cycle 3 ($t = 0.9375\text{ ns}$)**: CPU **speculatively jumps to $A_{\text{gadget}}$ in Kernel Mode ($PL=0$)**!
5. **Cycle 5 ($t = 1.5625\text{ ns}$)**: Gadget executes `mov rax, [rdi]`. Reads kernel secret byte $S = 77_{10} = \text{0x4D} = \text{'M'}$.
6. **Cycle 8 ($t = 2.5000\text{ ns}$)**: Gadget executes `mov rbx, [rax*64 + rsi]`. Dispatches load for probe line `T[77 * 64]`.
7. **Cycle 44 ($t = 13.7500\text{ ns}$)**: Line `T[77]` is fetched from L3 into L1 Data Cache.
8. **Cycle 160 ($t = 50.0000\text{ ns}$)**: Kernel function pointer arrives from DRAM (`0xFFFFFFFF_8105_2000`).
   * Misprediction detected! ($\text{Real Target } \text{0x8105\_2000} \neq \text{Predicted Target } \text{0x8100\_9000}$).
   * **ROB FLUSH FIRED!** Pipeline flushed. Registers reset.
   * **Line `T[77]` remains resident in L1 Data Cache!**

$$\text{Line Fill Complete Time } (44\text{ cycles}) \le \text{ROB Flush Time } (160\text{ cycles}) \quad (\mathbf{\text{SPECULATIVE LEAKAGE PASSED!}})$$

---

#### Step 3: Calculate Reload Timing Delta

The attacker reloads all 256 lines of `probe_array T`:
* **Un-accessed Lines $k \neq 77$**: Absent from cache $\implies T_{\text{DRAM}} = 180\text{ cycles}$.
* **Target Line $k = 77$**: Resident in L1 Data Cache $\implies T_{\text{L1\_hit}} = 4\text{ cycles}$.

$$\text{Timing Delta Saved } \Delta T = T_{\text{DRAM}} - T_{\text{L1\_hit}} = 180 - 4 = \mathbf{176 \text{ CPU Clock Cycles Saved!}}$$

The attacker measures a $176\text{-cycle}$ speedup on line 77, exfiltrating kernel secret byte **$S = 77 = \text{0x4D} = \text{'M'}$**!

---

#### Step 4: Verify Software BHB Clearing Mitigation

Now, suppose the Linux kernel executes a **BHB Clearing Loop** (32 dummy branches) on `syscall` entry:

##### 1. GHR State after BHB Clearing Loop:
The 32 dummy branch outcomes overwrite the GHR, setting $\text{GHR}_{\text{cleared}} = \mathbf{\text{0x000}}$.

##### 2. Recalculate Prediction Table Index ($I_{\text{safe}}$):

$$I_{\text{safe}} = A_{\text{kernel}}[15:4] \ \mathbf{\oplus} \ \text{GHR}_{\text{cleared}} = \text{0x104} \ \mathbf{\oplus} \ \text{0x000} = \mathbf{\text{0x104}}$$

##### 3. Security Outcome:
* $I_{\text{safe}} \, (\text{0x104}) \neq I_{\text{target}} \, (\text{0x123})$.
* The predictor queries Entry `0x104`, which points to the legitimate kernel function target `0x8105_2000`.
* **THE BHI ATTACK IS $100\%$ NEUTRALIZED!** Attacker's gadget $A_{\text{gadget}}$ is **NEVER** executed!

##### 4. Calculate Execution Time Penalty of BHB Clearing Loop:
Executing 32 dummy branches ($1\text{ cycle}$ per branch) plus loop overhead ($16\text{ cycles}$ total):

$$\text{Cycles}_{\text{BHB\_clearing}} = 32 + 16 = \mathbf{48 \text{ CPU Clock Cycles}}$$

In physical nanoseconds ($T_{\text{clk}} = 0.3125\text{ ns}$):

$$\Delta T_{\text{BHB\_clearing\_ns}} = 48 \times 0.3125 \text{ ns} = \mathbf{15.0 \text{ Nanoseconds}}$$

```text
BHB CLEARING MITIGATION PERFORMANCE & SECURITY SUMMARY

 Kernel Mode State             │ GHR State  │ Calculated Index │ Target Predicted │ BHI Attack Status
───────────────────────────────┼────────────┼──────────────────┼──────────────────┼────────────────────
 Un-Mitigated (No BHB Loop)    │ 0x027      │ Index 0x123      │ 0x8100_9000      │ HIJACKED (Gadget!)
 Mitigated (BHB Clearing Loop) │ 0x000      │ Index 0x104      │ 0x8105_2000      │ 100% SECURE!
```

##### Engineering Conclusion:
Executing the BHB Clearing Loop added **$15.0\text{ nanoseconds}$ ($48\text{ CPU clock cycles}$)** to every system call entry, but **completely eliminated Branch History Injection (BHI) vulnerabilities**, protecting kernel memory from speculative exfiltration!

---

### Sanity Check and Verification

Let us verify our mathematical, physical, and microarchitectural results against CPU design principles:

1. **Gshare Index XOR Math Check**:
   * $A_{\text{kernel}}[15:4] = \text{0x104} = 0001\_0000\_0100_2$.
   * $\text{GHR}_{\text{pattern}} = \text{0x027} = 0000\_0010\_0111_2$.
   * Index $= \text{0x104} \oplus \text{0x027} = 0001\_0010\_0011_2 = \text{0x123}$.
   * Index collision math verified with $100\%$ precision!
2. **eIBRS Bypassing Proof**:
   * Target Entry `0x123` was created by the kernel previously $\implies \text{Tag}_{\text{PL}} = 0$.
   * eIBRS privilege check ($\text{Tag}_{\text{PL}} == \text{Current\_PL}$) evaluated $0 == 0 \implies \mathbf{TRUE}$.
   * BHI bypassing of eIBRS hardware isolation mathematically proven!
3. **BHB Clearing Overwrite Check**:
   * 32 dummy branches shift 32 bits into the 32-bit GHR array.
   * All 32 bits of $\text{GHR}_{\text{pattern}}$ shifted out completely, leaving $\text{GHR} = \text{0x000}$.

All Gshare XOR index equations, eIBRS privilege tag verification steps, BHI speculative hijacking timelines, and BHB clearing cycle overheads evaluate with 100% mathematical, physical, and microarchitectural precision.

---

## Key Output Primitives

This lesson formally delivers the following conceptual primitives to your digital logic engineering toolbox:

* **Branch History Injection (BHI)**: A transient execution vulnerability where an unprivileged attacker process executes a sequence of conditional branches in user space to manipulate the un-isolated Global History Register (GHR), forcing kernel indirect branches to calculate index collisions that speculatively jump to kernel disclosure gadgets, bypassing eIBRS hardware isolation.
* **Global history register manipulation**: The microarchitectural technique of intentionally shifting specific Taken/Not-Taken branch outcomes into the shared hardware GHR shift register to control the XOR index lookup calculation of prediction tables in higher-privilege contexts.
